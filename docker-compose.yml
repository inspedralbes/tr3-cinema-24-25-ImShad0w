services:
  postgres:
    image: postgres:16-alpine
    container_name: cinema_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: cinema_user
      POSTGRES_PASSWORD: cinema_password
      POSTGRES_DB: cinema_db
    volumes:
      - ./db/db_data:/var/lib/postgresql/data

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    container_name: passmaster_frontend
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      - NEXT_PUBLIC_API_URL=/api
      - NEXT_PUBLIC_SOCKET_URL=

  backend:
    container_name: passmaster_backend
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    working_dir: /var/www
    volumes:
      - ./backend:/var/www
    ports:
      - "8002:8000"
    depends_on:
      - postgres

  realtime:
    build:
      context: ./realtime
      dockerfile: Dockerfile.prod
    container_name: passmaster_realtime
    restart: unless-stopped
    ports:
      - "3003:3001"
    volumes:
      - ./realtime:/app
      - /app/node_modules

#  nginx:
#    image: nginx:alpine
#    container_name: cinema_nginx
#    restart: unless-stopped
#    ports:
#      - "80:80"
#      - "443:443"
#    volumes:
#      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
#      - ./nginx/certs:/etc/nginx/certs:ro
#      - ./nginx/vhost.d:/etc/nginx/vhost.d
#    depends_on:
#      - backend
#      - frontend
#      - realtime

#  certbot:
#    image: certbot/certbot:latest
#    container_name: cinema_certbot
#    restart: unless-stopped
#    volumes:
#      - ./nginx/certs:/etc/nginx/certs:rw
#      - ./nginx/vhost.d:/etc/nginx/vhost.d:rw
#    entrypoint: "/bin/sh -c 'trap exit TERM; while true; do certbot renew --webroot -w /usr/share/nginx/html; sleep 12h & wait; done'"

volumes:
  postgres_data:
